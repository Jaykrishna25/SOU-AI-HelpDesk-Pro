# Viva Notes - superseded

This file was written during the initial build and describes behaviour that has
since changed (date-of-birth login, refresh tokens, AI accuracy figures).

**Use `VIVA_PREPARATION.md` instead.** It reflects the system as deployed and
includes the security issues found and fixed during development.
